You will need to run
pip install eyetrax paho-mqtt or pip install -r requirements.txt
to install the packages needed in this project.
From there you can run 
python3 main.py to run the input program or 
python3 robot_game_client.py to run the output game that the input source is controlling.
